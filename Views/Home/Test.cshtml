@{
    ViewData["Title"] = "Test Page";
}

<div id="app">
  {{ message }}
</div>

@section Scripts {
    <script>
        var app = new Vue({
            el: '#app',
            data: {
                message: 'Hello Vue!'
            },
            mounted() {
                console.log("Hey mountie");
                axios
                    .get('/api/products')
                    .then(response => (console.log(response.data)))
            }
        })
    </script>
}